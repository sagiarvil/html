# Change Control

Production validation behavior must change through versioned source files, tests and a reviewed release. No scanner rule may be silently changed by an upstream webpage at request time.
