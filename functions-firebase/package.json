{
  "name": "htmlandhtml-firebase-functions",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "engines": {"node": "22"},
  "scripts": {
    "sync-engine": "node scripts/sync-engine.mjs",
    "build": "npm run sync-engine && tsc -p tsconfig.json"
  },
  "dependencies": {
    "firebase-functions": "7.3.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.2"
  }
}
