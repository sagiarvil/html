# HTML&HTML Architecture

HTML&HTML uses an HTML-first commercial product model.

Default template stack:
- semantic HTML5
- modern CSS with custom properties
- lightweight Vanilla JavaScript when interaction is required
- no mandatory framework runtime

Quality targets include responsive behavior, accessibility, maintainability, commercial originality and simple customization. Claims are published only after the corresponding verification has run.
