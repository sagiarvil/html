(()=>{'use strict';const input=document.querySelector('[data-glossary-search]');const cards=[...document.querySelectorAll('[data-glossary-term]')];const count=document.querySelector('[data-glossary-count]');const empty=document.querySelector('[data-glossary-empty]');if(!input||!cards.length)return;const norm=s=>String(s||'').toLocaleLowerCase(document.documentElement.lang||'en').normalize('NFD').replace(/[\u0300-\u036f]/g,'');function run(){const q=norm(input.value.trim());let visible=0;for(const card of cards){const hay=norm(card.dataset.glossarySearch||card.textContent);const show=!q||hay.includes(q);card.hidden=!show;if(show)visible++}if(count)count.textContent=String(visible);if(empty)empty.classList.toggle('is-visible',visible===0)}input.addEventListener('input',run);run()})();
